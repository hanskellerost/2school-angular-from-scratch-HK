/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TodoPipe } from './todo.pipe';

describe('Pipe: Todoe', () => {
  it('create an instance', () => {
    let pipe = new TodoPipe();
    expect(pipe).toBeTruthy();
  });
});
